/**
 * Mismo criterio que `AdminUnderlineLabel` con `required` (modales de catálogo).
 */
export default function RequiredFieldMark() {
    return (
        <span className="ml-1 text-[11px] font-semibold leading-none text-[#C05050]">
            *
        </span>
    );
}
